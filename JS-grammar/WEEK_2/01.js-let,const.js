// 1. let, const

// before 2015, var was used to declare a variable
// after 2015, let, const were introduced

// 2. arrow function
function add() {}

var add = function () {};

var add = () => {
	return 1;
};

var add = (x) => 1;

// 3. Ternary Operator
// condition ? true : false
console.log(true ? "true" : "false"); // true
